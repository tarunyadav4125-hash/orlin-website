Orélin - React App

Steps to run locally:
1. npm install
2. npm start

This project is ready to upload to GitHub. After uploading, connect to Vercel (Import Git Repository) and deploy. Use React framework preset. Build command: npm run build. Output directory: build

WhatsApp number for orders is prefilled in the code (international format): +91 7419304795
